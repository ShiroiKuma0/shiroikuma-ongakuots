<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- 白い熊 音楽乙: the 保存復元 automation export runs in a foreground service. The v2 data door
         runs in a second one, typed specialUse — moving this app's own backup through a descriptor
         another app opened is not any of the platform's named categories, and the type is enforced
         from API 34 even though 白い熊's Mate XT reports SDK_INT 31. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Only Android 9 and older need this: there MediaStore still writes through shared
         storage. From Android 10 an app writing its own new image needs no permission, and
         SaveImagePermission grants without asking — so the declaration stops at 28 to match. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="androidx.car.app.MEDIA_TEMPLATES" />

    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.wifi"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.screen.portrait"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.screen.landscape"
        android:required="false" />
    <uses-feature
        android:name="android.software.car.templates_host.media"
        android:required="false" />

    <queries>
        <intent>
            <action android:name="android.media.action.DISPLAY_AUDIO_EFFECT_CONTROL_PANEL" />
        </intent>
    </queries>
    <queries>
        <package android:name="com.maxrave.simpmusic" />
    </queries>
    <!-- 白い熊 音楽乙 — 保存復元 contract v2 §4. Without naming the caller here, the reply
         broadcast's setPackage() fails SILENTLY on Android 11+: the export runs, writes correctly,
         and is never heard of. 自由作業盤 is named for the same reason on the §1 receiver's replies. -->
    <queries>
        <package android:name="shiroikuma.oyokanri" />
        <package android:name="shiroikuma.jiyusagyoban" />
    </queries>
    <queries>
        <intent>
            <action android:name="androidx.media3.session.MediaSessionService" />
        </intent>
    </queries>

    <application
        android:name=".SimpMusicApplication"
        android:allowBackup="true"
        android:appCategory="audio"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locale_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        android:enableOnBackInvokedCallback="true"
        android:requestLegacyExternalStorage="true"
        tools:replace="android:name"
        tools:targetApi="tiramisu">

        <activity
            android:name=".MainActivity"
            android:configChanges="keyboardHidden|screenSize|screenLayout|smallestScreenSize"
            android:exported="true"
            android:supportsPictureInPicture="true"
            android:enableOnBackInvokedCallback="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            tools:ignore="DiscouragedApi,LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MUSIC_PLAYER" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.APP_MUSIC" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Car App Library: host asks to surface the media playback screen
                 (minimized control panel tap, system media surfaces) -->
            <intent-filter>
                <action android:name="androidx.car.app.media.action.SHOW_MEDIA_PLAYBACK" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Youtube filter -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="youtube.com" />
                <data android:host="m.youtube.com" />
                <data android:host="www.youtube.com" />
                <data android:host="music.youtube.com" />
                <!-- video prefix -->
                <data android:pathPrefix="/v/" />
                <data android:pathPrefix="/embed/" />
                <data android:pathPrefix="/watch" />
                <!-- channel prefix -->
                <data android:pathPrefix="/channel/" />
                <data android:pathPrefix="/user/" />
                <data android:pathPrefix="/c/" />
                <!-- playlist prefix -->
                <data android:pathPrefix="/playlist" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="youtu.be" />
                <data android:pathPrefix="/" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="vnd.youtube" />
                <data android:scheme="vnd.youtube.launch" />
            </intent-filter>

            <!-- SimpMusic deep link filter (https) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:host="simpmusic.org" />
                <data android:pathPrefix="/app/" />
            </intent-filter>

            <!-- SimpMusic deep link filter (custom scheme) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="simpmusic" />
            </intent-filter>

            <!-- Last.fm auth callback. The scheme is fixed by the callback URL registered on the
                 Last.fm API account, so it is not "simpmusic". -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="lastfm-auth"
                    android:scheme="wordbyword" />
            </intent-filter>

            <!-- Share filter -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <meta-data
            android:name="androidx.car.app.TintableAttributionIcon"
            android:resource="@drawable/monochrome" />

        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>
        <service
            android:name="com.maxrave.media3.service.SimpleMediaService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            android:stopWithTask="false"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="android.media.browse.MediaBrowserService" />
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </service>
        <service
            android:name="com.maxrave.media3.service.download.MusicDownloadService"
            android:exported="true"
            android:foregroundServiceType="dataSync">
            <intent-filter>
                <action android:name="androidx.media3.exoplayer.downloadService.action.RESTART" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <receiver
            android:name="androidx.media3.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="8" />

        <!-- 白い熊 音楽乙: the Auto colour theme. CarColor.PRIMARY / SECONDARY resolve to the four
             carColor* attributes in this style (declared in :media3, which owns the car app code),
             so every template element the app tints comes out in the house yellow. The host still
             draws its own backgrounds and text colours — that part is not ours to set. -->
        <meta-data
            android:name="androidx.car.app.theme"
            android:resource="@style/ShiroikumaCarAppTheme" />

        <service
            android:name="com.maxrave.media3.carapp.SimpMusicCarAppService"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <action android:name="androidx.car.app.media.action.SHOW_MEDIA_PLAYBACK" />
                <category android:name="androidx.car.app.category.MEDIA" />
            </intent-filter>
        </service>


        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />


        <receiver android:name=".ui.widget.TurntableWidgetReceiver"
            android:label="@string/widget_turntable_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/turntable_widget_info" />
        </receiver>

        <receiver android:name=".ui.widget.PlaylistsWidgetReceiver"
            android:label="@string/widget_playlists_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/playlists_widget_info" />
        </receiver>

        <receiver android:name=".ui.widget.ListeningInsightsWidgetReceiver"
            android:label="@string/widget_insights_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/insights_widget_info" />
        </receiver>

        <!-- 白い熊 音楽乙 — the 保存復元 state-export automation.
             The receiver is exported and carries NO android:permission: the caller cannot hold one,
             so the token is the gate. It does no work itself — a manifest receiver that outstays the
             broadcast window is ANR'd and killed mid-export — and hands off to the service below,
             which is not exported and is reached only through the receiver. That is also why CANCEL
             is an action on the receiver rather than a call into the service: a third-party app
             cannot start a non-exported service. -->
        <receiver
            android:name="com.maxrave.simpmusic.shiroikuma.automation.StateExportReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="shiroikuma.ongakuots.action.EXPORT_STATE" />
                <action android:name="shiroikuma.ongakuots.action.LIST_CATEGORIES" />
                <action android:name="shiroikuma.ongakuots.action.CANCEL_EXPORT" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.maxrave.simpmusic.shiroikuma.automation.StateExportService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 白い熊 音楽乙 — the 保存復元 contract v2 data door.
             Exported with NO android:permission, and for a real reason rather than the receiver's:
             a custom permission would have to be `normal` (any app may hold it) or `signature`
             (impossible — every app in this family has its own keystore, 42 of them). Neither buys
             anything AutomationCallers does not already check, which is an exact package name, a uid
             cross-check and a pinned signing certificate. -->
        <provider
            android:name="com.maxrave.simpmusic.shiroikuma.automation.AutomationProvider"
            android:authorities="${applicationId}.automation"
            android:exported="true" />

        <service
            android:name="com.maxrave.simpmusic.shiroikuma.automation.AutomationDataService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Automation data export/import" />
        </service>

        <!-- Capability discovery. Read with MATCH_UNINSTALLED_PACKAGES | MATCH_DISABLED_COMPONENTS,
             because 白い熊 freezes apps aggressively and a frozen app cannot be ASKED anything — a
             query intent would make this app vanish from 応用管理's list exactly when it is disabled.
             An app without these entries is simply not offered for data backup or restore, which is
             the opt-out: implement nothing and nothing is claimed on your behalf. -->
        <meta-data
            android:name="shiroikuma.automation.contract"
            android:value="2" />
        <meta-data
            android:name="shiroikuma.automation.format"
            android:value="1" />
        <meta-data
            android:name="shiroikuma.automation.min_format"
            android:value="1" />

    </application>

</manifest>